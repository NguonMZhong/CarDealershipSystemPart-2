package org.yearup.models;

import static org.junit.jupiter.api.Assertions.*;

class LeaseContractTest
{

    @org.junit.jupiter.api.Test
    public void getMonthlyPayment()
    /*{
        // Arrange
        //Create leaseContract object to test
        LeaseContract leaseContract = new LeaseContract("2014-05-23","John Smith",
                "johnsmit@test.com", new Vehicle("4500",2015,"Lexus","RX",
                "SUV","Gold",2300,24_000), 1200,.07);

        double originalPrice = leaseContract.getVehicleSold().getPrice();
        int leaseTerm = 36;

        double expectedEndValue = 0.5 * originalPrice;
        double expectedMonthlyPayment = (originalPrice - expectedEndValue) / leaseTerm;

        // Act
        double actualMonthlyPayment = leaseContract.getMonthlyPayment();

        // Assert that the calculated monthly payment matches the expected value
        assertEquals(expectedMonthlyPayment, actualMonthlyPayment, 0.001);
    }

     */
}